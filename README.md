# Quadis Hotels

Follow these steps to clone the repository and run the web application locally on your device:

```bash
git clone https://github.com/DsThakurRawat/Quadis.git
cd Quadis
npm install
npm run dev
```
